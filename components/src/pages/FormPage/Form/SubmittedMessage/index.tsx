import React from 'react';
import styles from './style.module.scss';

const SubmittedMessage = () => (
  <div className={styles.submittedMessage}>Card has been saved</div>
);

export default SubmittedMessage;
